@{
    Layout="Shared/_Layout.cshtml";
}


@if(Model.Count != 0)
{
    @foreach(var ticket in Model)
    {
        <a href="/ticket/@ticket.GetId()/"><p>Ticket#: @ticket.GetTicketNumber()</p></a>
    }
}
